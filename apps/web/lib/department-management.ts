import { createAdminClient } from "./supabase/admin";
import { getOrganizationContext, requireOrganizationPermission } from "./organization-context";
export type Department={id:string;organizationId:string;branchId:string;branchName:string;code:string;name:string;status:"ACTIVE"|"INACTIVE"};
async function context(){const c=await getOrganizationContext();if(!c)throw new Error("UNAUTHENTICATED");requireOrganizationPermission(c,"department.manage");return c;}
export async function listDepartments(){const c=await context();const {data,error}=await createAdminClient().from("departments").select("id,organization_id,branch_id,code,name,status,branches!inner(name)").eq("organization_id",c.organizationId).order("name");if(error)throw new Error(error.message);return(data??[]).map((d:any)=>({id:d.id,organizationId:d.organization_id,branchId:d.branch_id,branchName:d.branches?.name??"",code:d.code,name:d.name,status:d.status})) as Department[];}
export async function getDepartment(id:string){const c=await context();const {data,error}=await createAdminClient().from("departments").select("id,organization_id,branch_id,code,name,status,branches!inner(name)").eq("id",id).eq("organization_id",c.organizationId).maybeSingle();if(error)throw new Error(error.message);if(!data)return null;return {id:data.id,organizationId:data.organization_id,branchId:data.branch_id,branchName:data.branches?.name??"",code:data.code,name:data.name,status:data.status} as Department;}
export async function createDepartment(input:Omit<Department,"id"|"organizationId"|"branchName">){const c=await context();const admin=createAdminClient();const {data:branch}=await admin.from("branches").select("id").eq("id",input.branchId).eq("organization_id",c.organizationId).maybeSingle();if(!branch)throw new Error("BRANCH_NOT_FOUND");const {data,error}=await admin.from("departments").insert({organization_id:c.organizationId,branch_id:input.branchId,code:input.code.trim().toUpperCase(),name:input.name.trim(),status:input.status||"ACTIVE"}).select("id,organization_id,branch_id,code,name,status").single();if(error)throw new Error(error.message);return data;}
export async function updateDepartment(id:string,input:Partial<Omit<Department,"id"|"organizationId"|"branchName">>){const c=await context();const admin=createAdminClient();const {data:existing}=await admin.from("departments").select("id").eq("id",id).eq("organization_id",c.organizationId).maybeSingle();if(!existing)throw new Error("DEPARTMENT_NOT_FOUND");if(input.branchId){const {data:branch}=await admin.from("branches").select("id").eq("id",input.branchId).eq("organization_id",c.organizationId).maybeSingle();if(!branch)throw new Error("BRANCH_NOT_FOUND");}const patch:any={};if(input.branchId!==undefined)patch.branch_id=input.branchId;if(input.code!==undefined)patch.code=input.code.trim().toUpperCase();if(input.name!==undefined)patch.name=input.name.trim();if(input.status!==undefined)patch.status=input.status;const {data,error}=await admin.from("departments").update(patch).eq("id",id).eq("organization_id",c.organizationId).select("id,organization_id,branch_id,code,name,status").single();if(error)throw new Error(error.message);return data;}
