"use client";
import { useState } from "react";
import Link from "next/link";
export default function NewBranchPage(){const [error,setError]=useState("");const [saving,setSaving]=useState(false);async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();setSaving(true);setError("");const form=new FormData(e.currentTarget);const payload={code:form.get("code"),name:form.get("name"),address:form.get("address"),contactNo:form.get("contactNo"),email:form.get("email"),isDefault:form.get("isDefault")==="on"};const r=await fetch("/api/organization/branches",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify(payload)});if(!r.ok){const j=await r.json().catch(()=>({}));setError(j.error||"Unable to create branch");setSaving(false);return}window.location.href="/clinic/branches";}return <main className="workspace-page"><header className="workspace-header"><div><p className="eyebrow">Branch management</p><h1>Add branch</h1><p className="muted">Create an operational location under the current organization.</p></div><Link href="/clinic/branches">Cancel</Link></header><form className="panel form-grid" onSubmit={submit}><label>Branch name<input name="name" required maxLength={150}/></label><label>Branch code<input name="code" required maxLength={50}/></label><label>Address<textarea name="address" rows={3}/></label><label>Contact number<input name="contactNo"/></label><label>Email<input name="email" type="email"/></label><label><input name="isDefault" type="checkbox"/> Set as default branch</label>{error&&<p role="alert">{error}</p>}<button className="primary-button" disabled={saving}>{saving?"Saving…":"Create branch"}</button></form></main>}
