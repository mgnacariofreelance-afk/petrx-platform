import Link from "next/link";
import { updateDepartment } from "../../../../../lib/department-management";
import { listBranches } from "../../../../../lib/branch-management";
import { createAdminClient } from "../../../../../lib/supabase/admin";
export const dynamic="force-dynamic";
async function action(formData:FormData){"use server";await updateDepartment(String(formData.get("id")),{branchId:String(formData.get("branchId")),code:String(formData.get("code")),name:String(formData.get("name")),status:String(formData.get("status")) as "ACTIVE"|"INACTIVE"});}
export default async function EditDepartment({params}:{params:Promise<{id:string}>}){const {id}=await params;const [{data:d},branches]=await Promise.all([createAdminClient().from("departments").select("id,code,name,branch_id,status").eq("id",id).maybeSingle(),listBranches()]);if(!d)return <main className="workspace-page"><section className="panel"><h1>Department not found</h1><Link href="/clinic/departments">Back to departments</Link></section></main>;return <main className="workspace-page"><header className="workspace-header"><div><p className="eyebrow">Organization</p><h1>Edit Department</h1></div><Link className="secondary-button" href="/clinic/departments">Back</Link></header><section className="panel"><form action={action} className="form-grid"><input type="hidden" name="id" value={d.id}/><label>Department code<input name="code" defaultValue={d.code} required maxLength={30}/></label><label>Department name<input name="name" defaultValue={d.name} required maxLength={120}/></label><label>Branch<select name="branchId" defaultValue={d.branch_id} required>{branches.map(b=><option value={b.id} key={b.id}>{b.name} ({b.code})</option>)}</select></label><label>Status<select name="status" defaultValue={d.status}><option value="ACTIVE">Active</option><option value="INACTIVE">Inactive</option></select></label><div><button className="primary-button" type="submit">Save changes</button></div></form></section></main>}
